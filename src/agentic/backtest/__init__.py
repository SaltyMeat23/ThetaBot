"""Historical CSP backtest — replays real option prices through the LIVE screening logic.

See engine.CspBacktest. Results validate mechanics + support relative tuning; they are NOT proof
of profitability (Alpaca's ~2yr window has no crash, greeks are BS-computed, slippage estimated).
"""
